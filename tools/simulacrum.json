{
  "name": "Simulacrum",
  "description": "Launch a new agent that has access to the following tools: View, Glob, Grep, Ls.",
  "parameters": {
    "type": "object",
    "required": ["prompt"],
    "properties": {
      "prompt": {
        "type": "string",
        "description": "The task for the agent to perform"
      }
    }
  }
}
